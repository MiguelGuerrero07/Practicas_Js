const producto = {
    nombre: "Monitor 20 Pulgadas",
    precio: 300,
    disponible : true,
}

console.log(Object.keys(producto)); //Retorna  la parte izquierda del objecto


console.log(Object.values(producto)); //Retorna  la parte derecha del objecto


console.log(Object.entries(producto)); //Retorna todo